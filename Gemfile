source "https://rubygems.org"

gemspec
# ask-mcp is resolved from rubygems via the gemspec floor `>= 0.4` (the 0.4
# line carries the stateless 2026-07-28 protocol support).

group :test do
  gem "minitest", "~> 5.25"
  gem "rake", "~> 13.0"
end
